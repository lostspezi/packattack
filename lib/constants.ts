/**
 * Starter coins granted once at signup. Sized to match the onboarding
 * tour's tutorial box exactly so a new user can complete the tour using
 * only their signup balance. The tour grants another 10 coins on first
 * completion, leaving the user at 10 after finishing.
 */
export const SIGNUP_BONUS_COINS = 10;
export const TOUR_REWARD_COINS = 10;
